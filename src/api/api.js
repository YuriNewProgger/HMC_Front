//export const server = process.env.NODE_ENV === "development" ? `http://192.168.1.7:8000` : ""; //адрес сервера
export const server = process.env.NODE_ENV === "development" ? `http://127.0.0.1:8000` : ""; //адрес сервера
//export const server = `http://127.0.0.1:8000`; //адрес сервера